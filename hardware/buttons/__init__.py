"""
These files contains common code for checking if a "button" is pressed. Note 
that button doesn't necessarily mean a physical button. It could just be a
some sort of signal that we send when we start or stop recording. 
"""
from buttons.base_button import BaseButton
from buttons.file_button import FileButton 
assert BaseButton
assert FileButton
